<nav class="navbar fixed-top navbar-expand-md navbar-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">
            <img src="{{ asset('build/images/logo.png') }}" alt="Logo MusicAll" />
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarCollapse">
            <form class="form-inline ml-auto">
                <input class="form-control mr-sm-2" type="text" placeholder="Rechercher" aria-label="Search">
            </form>

            {% if app.user %}
                <div id="user-data" data-username="{{ app.user.username }}"></div>
                <div id="nav-bar-dropdown"></div>
            {% else %}
                <a href="{{ path('app_register') }}" class="ml-auto btn btn-sm btn-registration">s'inscrire</a>
                <a href="{{ path('app_login') }}" class="ml-2 btn btn-sm btn-login">se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>
