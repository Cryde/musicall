api_login_check:
    path: /api/login_check
    options:
        expose: true
    defaults:
        _format: json

api_refresh_token:
    path: /api/token/refresh
    options:
        expose: true

controllers:
    resource: ../src/Controller/
    type: attribute
