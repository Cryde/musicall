api_login_check:
    path: /api/login_check
    options:
        expose: true
    defaults:
        _format: json

controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute
