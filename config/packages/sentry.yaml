sentry:
    dsn: '%env(SENTRY_DSN)%'
    options:
        environment: '%kernel.environment%'
        excluded_exceptions:
            - Symfony\Component\Security\Core\Exception\AccessDeniedException
