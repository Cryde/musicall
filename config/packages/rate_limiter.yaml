framework:
    rate_limiter:
        contact_form:
            policy: sliding_window
            limit: 3
            interval: '1 hour'
            cache_pool: "rate_limiter.cache"
        musician_search:
            policy: sliding_window
            limit: 10
            interval: '20 minutes'
            cache_pool: "rate_limiter.cache"
        registration:
            policy: fixed_window
            limit: 3
            interval: '1 hour'
            cache_pool: "rate_limiter.cache"
        message_send:
            policy: fixed_window
            limit: 20
            interval: '1 minute'
            cache_pool: "rate_limiter.cache"
        thread_creation:
            policy: fixed_window
            limit: 5
            interval: '1 minute'
            cache_pool: "rate_limiter.cache"
